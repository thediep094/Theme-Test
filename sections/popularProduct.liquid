{% assign collection = collections['all'] %}
{{collection.title}}
{% for product in collection.products %}
	<div>
		{{product.images}}
        {{product.title}}
    </div>
{% endfor %}

{% schema %}
  {
    "name": "Section name",
    "settings": []
  }
{% endschema %}

