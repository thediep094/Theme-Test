{% assign collection = collections['all'] %}
{{collection.title}}
{% for product in collection.products %}
	<div>
 		<img src="{{product.media | where: "alt", "primary-image"}}" alt="">
        {{product.title}}
    </div>
{% endfor %}

{% schema %}
  {
    "name": "Section name",
    "settings": []
  }
{% endschema %}

